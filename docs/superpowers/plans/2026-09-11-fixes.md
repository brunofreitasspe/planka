# Lote de correções (datas, busca, clipboard, PDF) — Implementation Plan

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** Corrigir os 5 problemas relatados — formato de data americano, input manual de data quebrando, busca que ignora custom fields, botão de copiar inoperante e PDF de exportação cortando conteúdo.

**Architecture:** Cinco correções aplicadas na ordem 1→5, um commit por item, na branch `fixes`. Itens 1 e 2 têm a mesma origem (locale/formato de data). Item 3 tem duas implementações paralelas (client em `Board.js`, server em `Card.js`) que precisam da mesma semântica. Item 5 tem um gate: um mockup HTML é validado antes de tocar no gerador de PDF.

**Tech Stack:** Sails.js + Knex (server), React + Redux-ORM + i18next (client), `pdfkit` (PDF), `react-datepicker` v9, `date-fns` v4 (client) / `moment` (server), Jest + Testing Library (client), Mocha + Chai (server).

## Global Constraints

- Branch de trabalho: `fixes`, criada a partir de `master`. Um commit por item, ordem 1→5.
- Locale padrão do sistema: `pt-BR`.
- Formato de data exibido em todo o sistema: `dd/MM/yyyy`.
- Tabela de usuários chama-se **`user_account`** (não `user`); a coluna de idioma é `language`.
- Client tests: Jest, colocalizados como `*.test.js` ao lado do fonte. Rodar com `npm test` dentro de `client/`.
- Server tests: Mocha + Chai, em `server/test/utils/*.test.js` (ou `test/integration/**`). Rodar com `npm test` dentro de `server/`.
- Lint: `npm run lint` em cada pacote deve sair com código 0. O projeto usa `--max-warnings=0` no server.
- Copyright header obrigatório em todo arquivo novo (copiar de um arquivo existente do mesmo diretório).

---

## Estrutura de arquivos

| Arquivo | Responsabilidade | Ação |
|---|---|---|
| `client/src/i18n.js` | Bootstrap do i18next; define o locale padrão | Modificar |
| `client/src/locales/pt-BR/index.js` | Locale pt-BR (passa a ser o embarcado) | Modificar |
| `client/src/locales/en-US/index.js` | Locale en-US (deixa de ser embarcado) | Modificar |
| `client/src/locales/index.test.js` | Garante qual locale está embarcado e seu formato de data | Criar |
| `client/src/sagas/core/services/core.js` | Aplica `user.language` no login | Modificar |
| `server/db/migrations/20260911120000_set_default_language_to_pt_br.js` | Reseta `user_account.language` para pt-BR | Criar |
| `client/src/utils/custom-field-date.js` | Parse/formatação/validação da data de custom field | Criar |
| `client/src/utils/custom-field-date.test.js` | Testes do anterior | Criar |
| `client/src/components/custom-fields/CustomField/DateValueField.jsx` | Input de data do custom field | Modificar |
| `client/src/utils/build-card-search-strings.js` | Strings buscáveis de um card (inclui custom fields) | Criar |
| `client/src/utils/build-card-search-strings.test.js` | Testes do anterior | Criar |
| `client/src/models/Board.js` | Busca do board (client) | Modificar |
| `server/utils/custom-field-search.js` | Fragmento SQL da busca em custom fields | Criar |
| `server/test/utils/custom-field-search.test.js` | Testes do anterior | Criar |
| `server/api/hooks/query-methods/models/Card.js` | Busca server-side | Modificar |
| `client/src/utils/copy-to-clipboard.js` | Cópia com fallback para contexto não-seguro | Criar |
| `client/src/utils/copy-to-clipboard.test.js` | Testes do anterior | Criar |
| `client/src/components/custom-fields/CustomField/CustomField.jsx` | Botão de copiar | Modificar |
| `docs/mockup-pdf-export.html` | Mockup validável do novo layout do PDF | Criar |
| `server/utils/label-colors.js` | Mapa `label.color` → hex + cor de texto | Criar |
| `server/utils/export-formatters.js` | Gerador de PDF/CSV | Modificar |
| `server/test/utils/export-formatters.test.js` | Testes dos formatters | Modificar |

---

## Task 1: pt-BR como idioma padrão do sistema

**Files:**
- Modify: `client/src/i18n.js`
- Modify: `client/src/locales/pt-BR/index.js`
- Modify: `client/src/locales/en-US/index.js`
- Modify: `client/src/sagas/core/services/core.js:48`
- Create: `client/src/locales/index.test.js`
- Create: `server/db/migrations/20260911120000_set_default_language_to_pt_br.js`

**Interfaces:**
- Consumes: nada
- Produces: `FALLBACK_LANGUAGE` exportado nomeado por `client/src/i18n.js` (string `'pt-BR'`), usado pela Task 1 e disponível para o resto do client.

**Contexto:** `pt-BR/core.js` já define `format.date = 'dd/MM/yyyy'`. O problema é só *qual* locale é o padrão. Hoje `FALLBACK_LANGUAGE = 'en-US'` e `LanguageDetector` sobrescreve o fallback com o idioma do navegador — por isso remover o detector também é necessário: sem isso, um usuário anônimo com navegador em inglês continuaria em en-US.

- [ ] **Step 1: Escrever o teste que falha**

Criar `client/src/locales/index.test.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import { embeddedLocales } from './index';

describe('embeddedLocales', () => {
  test('pt-BR is embedded with the core bundle available offline', () => {
    expect(embeddedLocales['pt-BR'].translation).toBeDefined();
    expect(embeddedLocales['pt-BR'].translation.common.language).toBeDefined();
  });

  test('pt-BR formats dates as DD/MM/YYYY', () => {
    expect(embeddedLocales['pt-BR'].format.date).toBe('dd/MM/yyyy');
  });

  test('en-US no longer carries the core bundle', () => {
    expect(embeddedLocales['en-US'].format).toBeUndefined();
  });
});
```

- [ ] **Step 2: Rodar o teste e verificar que falha**

Run: `cd client && npx jest src/locales/index.test.js`
Expected: FAIL — `embeddedLocales['pt-BR'].translation` é `undefined` (hoje pt-BR só embarca `login`) e `embeddedLocales['en-US'].format` existe.

- [ ] **Step 3: Embarcar pt-BR e liberar en-US**

Substituir o conteúdo de `client/src/locales/pt-BR/index.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import merge from 'lodash/merge';

import login from './login';
import core from './core';

export default {
  language: 'pt-BR',
  country: 'br',
  name: 'Português',
  embeddedLocale: merge(login, core),
};
```

Substituir o conteúdo de `client/src/locales/en-US/index.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import login from './login';

export default {
  language: 'en-US',
  country: 'us',
  name: 'English',
  embeddedLocale: login,
};
```

- [ ] **Step 4: Rodar o teste e verificar que passa**

Run: `cd client && npx jest src/locales/index.test.js`
Expected: PASS — 3 testes.

- [ ] **Step 5: Trocar o fallback e remover o detector de idioma**

Em `client/src/i18n.js`:

Remover a linha do import do detector (linha 7):

```js
import LanguageDetector from 'i18next-browser-languagedetector';
```

Trocar a linha 23:

```js
const FALLBACK_LANGUAGE = 'en-US';
```

por:

```js
export const FALLBACK_LANGUAGE = 'pt-BR';
```

Remover a chamada `.use(LanguageDetector)` da cadeia de `i18n.init` (linha 106), deixando:

```js
i18n
  .use(formatDatePostProcessor)
  .use(parseDatePostProcessor)
  .use(initReactI18next)
  .init({
```

- [ ] **Step 6: Garantir que `user.language` nulo caia em pt-BR**

Em `client/src/sagas/core/services/core.js`, adicionar o import (junto aos demais):

```js
import { FALLBACK_LANGUAGE } from '../../../i18n';
```

Trocar a linha 48:

```js
  yield call(i18n.changeLanguage, user.language);
```

por:

```js
  yield call(i18n.changeLanguage, user.language || FALLBACK_LANGUAGE);
```

- [ ] **Step 7: Verificar lint e suíte do client**

Run: `cd client && npm run lint`
Expected: exit 0.

Run: `cd client && npm test`
Expected: PASS (inclui `src/locales/index.test.js`, `src/constants/CardPriorities.test.js`, `src/utils/local-id.test.js`).

- [ ] **Step 8: Criar a migration de idioma**

Criar `server/db/migrations/20260911120000_set_default_language_to_pt_br.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

// One-time reset of the stored language preference to the new system default.
// Deliberately not reversible: the previous per-user values are not recoverable,
// and every user can pick a different language again in User Settings → Account.
exports.up = (knex) =>
  knex('user_account').update({
    language: 'pt-BR',
  });

// eslint-disable-next-line no-empty-function
exports.down = () => {};
```

- [ ] **Step 9: Aplicar a migration e conferir o resultado**

Run: `cd server && npm run db:migrate`
Expected: a migration `20260911120000_set_default_language_to_pt_br.js` aparece como aplicada.

Conferir no banco (ajustar credenciais conforme `.env`):

```sql
SELECT language, COUNT(*) FROM user_account GROUP BY language;
```

Expected: uma única linha, `pt-BR`, com o total de usuários.

- [ ] **Step 10: Commit**

```bash
git add client/src/i18n.js client/src/locales/index.test.js \
  client/src/locales/pt-BR/index.js client/src/locales/en-US/index.js \
  client/src/sagas/core/services/core.js \
  server/db/migrations/20260911120000_set_default_language_to_pt_br.js
git commit -m "fix: pt-BR como idioma padrão e formato de data dd/MM/yyyy"
```

---

## Task 2: Input manual de data no custom field

**Files:**
- Create: `client/src/utils/custom-field-date.js`
- Create: `client/src/utils/custom-field-date.test.js`
- Modify: `client/src/components/custom-fields/CustomField/DateValueField.jsx`

**Interfaces:**
- Consumes: nada
- Produces:
  - `parseCustomFieldDate(value: string | null | undefined) → Date | null`
  - `formatCustomFieldDate(date: Date | null | undefined) → string | null`
  - `isValidCustomFieldDate(date: Date | null | undefined) → boolean`

**Contexto:** `react-datepicker` v9 fixa `dateFormat: "MM/dd/yyyy"` em `defaultProps` e o componente usa `props.dateFormat ?? DatePicker.defaultProps.dateFormat`. Como `DateValueField.jsx` não passa `dateFormat`, o input lê/escreve em formato americano independente do locale. Digitar `25/12/2026` faz o parser ler mês `25` → `Invalid Date`, e o `formatIsoDate` atual monta a string `NaN-NaN-NaN`, que é enviada ao servidor.

- [ ] **Step 1: Escrever o teste que falha**

Criar `client/src/utils/custom-field-date.test.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import {
  formatCustomFieldDate,
  isValidCustomFieldDate,
  parseCustomFieldDate,
} from './custom-field-date';

describe('parseCustomFieldDate', () => {
  test('parses an ISO date as a local date, not UTC', () => {
    const date = parseCustomFieldDate('2026-09-10');

    expect(date.getFullYear()).toBe(2026);
    expect(date.getMonth()).toBe(8);
    expect(date.getDate()).toBe(10);
  });

  test('returns null for empty input', () => {
    expect(parseCustomFieldDate(null)).toBeNull();
    expect(parseCustomFieldDate('')).toBeNull();
    expect(parseCustomFieldDate(undefined)).toBeNull();
  });

  test('returns null for a non-date string', () => {
    expect(parseCustomFieldDate('not-a-date')).toBeNull();
  });
});

describe('formatCustomFieldDate', () => {
  test('formats as YYYY-MM-DD with zero padding', () => {
    expect(formatCustomFieldDate(new Date(2026, 8, 10))).toBe('2026-09-10');
    expect(formatCustomFieldDate(new Date(2026, 0, 5))).toBe('2026-01-05');
  });

  test('returns null for an invalid date instead of NaN-NaN-NaN', () => {
    expect(formatCustomFieldDate(new Date('nope'))).toBeNull();
    expect(formatCustomFieldDate(null)).toBeNull();
    expect(formatCustomFieldDate(undefined)).toBeNull();
  });
});

describe('isValidCustomFieldDate', () => {
  test('accepts a real date', () => {
    expect(isValidCustomFieldDate(new Date(2026, 8, 10))).toBe(true);
  });

  test('rejects an invalid date and non-dates', () => {
    expect(isValidCustomFieldDate(new Date('nope'))).toBe(false);
    expect(isValidCustomFieldDate(null)).toBe(false);
    expect(isValidCustomFieldDate(undefined)).toBe(false);
    expect(isValidCustomFieldDate('2026-09-10')).toBe(false);
  });
});
```

- [ ] **Step 2: Rodar o teste e verificar que falha**

Run: `cd client && npx jest src/utils/custom-field-date.test.js`
Expected: FAIL — `Cannot find module './custom-field-date'`.

- [ ] **Step 3: Criar o util**

Criar `client/src/utils/custom-field-date.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import isValid from 'date-fns/isValid';

// A custom field DATE is stored as a plain 'YYYY-MM-DD' string. Building the Date
// with an explicit time avoids the UTC shift `new Date('2026-09-10')` would apply.
export const parseCustomFieldDate = (value) => {
  if (!value) {
    return null;
  }

  const date = new Date(`${value}T00:00:00`);

  return isValid(date) ? date : null;
};

export const isValidCustomFieldDate = (date) =>
  !!date && date instanceof Date && isValid(date);

export const formatCustomFieldDate = (date) => {
  if (!isValidCustomFieldDate(date)) {
    return null;
  }

  return [
    date.getFullYear(),
    `${date.getMonth() + 1}`.padStart(2, '0'),
    `${date.getDate()}`.padStart(2, '0'),
  ].join('-');
};
```

- [ ] **Step 4: Rodar o teste e verificar que passa**

Run: `cd client && npx jest src/utils/custom-field-date.test.js`
Expected: PASS — 7 testes.

- [ ] **Step 5: Usar o util no componente e corrigir o formato do input**

Substituir o conteúdo de `client/src/components/custom-fields/CustomField/DateValueField.jsx`:

```jsx
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import React, { useCallback } from 'react';
import PropTypes from 'prop-types';
import { useTranslation } from 'react-i18next';
import DatePicker from 'react-datepicker';

import {
  formatCustomFieldDate,
  isValidCustomFieldDate,
  parseCustomFieldDate,
} from '../../../../utils/custom-field-date';

import styles from './DateValueField.module.scss';

const DateValueField = React.memo(({ defaultValue, onUpdate }) => {
  const [t] = useTranslation();

  const handleChange = useCallback(
    (date) => {
      if (date === null) {
        onUpdate(null);
        return;
      }

      // react-datepicker hands back an Invalid Date while the user is mid-typing.
      // Reporting that upstream would persist the string 'NaN-NaN-NaN'.
      if (!isValidCustomFieldDate(date)) {
        return;
      }

      onUpdate(formatCustomFieldDate(date));
    },
    [onUpdate],
  );

  return (
    <DatePicker
      isClearable
      className={styles.field}
      dateFormat={t('format:date')}
      selected={parseCustomFieldDate(defaultValue)}
      onChange={handleChange}
    />
  );
});

DateValueField.propTypes = {
  defaultValue: PropTypes.string,
  onUpdate: PropTypes.func.isRequired,
};

DateValueField.defaultProps = {
  defaultValue: undefined,
};

export default DateValueField;
```

- [ ] **Step 6: Verificar lint**

Run: `cd client && npm run lint`
Expected: exit 0. Se o lint acusar `import/isEmpty` não usado, confirme que a linha foi removida do util (Step 3).

- [ ] **Step 7: Verificar manualmente no navegador**

Run: `cd client && npm start` (com o server já rodando)

1. Abrir um card com um custom field do tipo Data.
2. Digitar manualmente `25/12/2026` no campo e sair do campo (blur).
3. Expected: a data é aceita e exibida como `25/12/2026`; recarregar o card mostra `25/12/2026` persistido.
4. Digitar `32/13/2026` e sair do campo.
5. Expected: nothing is saved; o valor anterior permanece (nenhum `NaN-NaN-NaN`).

- [ ] **Step 8: Commit**

```bash
git add client/src/utils/custom-field-date.js \
  client/src/utils/custom-field-date.test.js \
  client/src/components/custom-fields/CustomField/DateValueField.jsx
git commit -m "fix: aceita digitação manual de data no custom field (DD/MM/YYYY)"
```

---

## Task 3: Busca do board incluindo custom fields

**Files:**
- Create: `client/src/utils/build-card-search-strings.js`
- Create: `client/src/utils/build-card-search-strings.test.js`
- Modify: `client/src/models/Board.js:341-375`
- Create: `server/utils/custom-field-search.js`
- Create: `server/test/utils/custom-field-search.test.js`
- Modify: `server/api/hooks/query-methods/models/Card.js:75-91`

**Interfaces:**
- Consumes: `CustomFieldTypes` (`client/src/constants/CustomFieldTypes.js`) — valores `text`, `number`, `date`, `checkbox`, `dropdown`.
- Produces (client): `buildCardSearchStrings(cardModel) → string[]` — todas as strings em minúsculas, já normalizadas por tipo, prontas para `includes()`.
- Produces (server): `buildCustomFieldSearchClause({ startIndex }) → { sql: string, values: string[] }` — fragmento SQL que começa em `$startIndex`, com os valores na ordem correspondente.

**Contexto:** A busca existe em dois lugares e ambos ignoram custom fields. `custom_field_value` tem `card_id`, `custom_field_id`, `content`. O `content` guarda: texto livre (TEXT), número como string (NUMBER), ISO `YYYY-MM-DD` (DATE), id da opção (DROPDOWN), `'true'`/`'false'` (CHECKBOX).

Decisões: DATE precisa casar também na forma `DD/MM/YYYY` (senão buscar `10/09/2026` nunca acha nada) e DROPDOWN precisa casar pelo **nome** da opção, não pelo id. CHECKBOX fica fora — `'true'` é ruído.

- [ ] **Step 1: Escrever o teste do client que falha**

Criar `client/src/utils/build-card-search-strings.test.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import buildCardSearchStrings from './build-card-search-strings';

const buildCard = ({ name = '', description = '', values = [] } = {}) => ({
  name,
  description,
  customFieldValues: {
    toModelArray: () =>
      values.map(([type, content, options]) => ({
        content,
        customField: {
          type,
          config: options ? { options } : undefined,
        },
      })),
  },
});

describe('buildCardSearchStrings', () => {
  test('includes name and description lowercased', () => {
    const result = buildCardSearchStrings(
      buildCard({ name: 'Falha no Login', description: 'Erro 500' }),
    );

    expect(result).toContain('falha no login');
    expect(result).toContain('erro 500');
  });

  test('includes text custom field values', () => {
    const result = buildCardSearchStrings(buildCard({ values: [['text', 'HMMG-2026-001']] }));

    expect(result).toContain('hmmg-2026-001');
  });

  test('includes number custom field values', () => {
    const result = buildCardSearchStrings(buildCard({ values: [['number', '42500']] }));

    expect(result).toContain('42500');
  });

  test('includes a date custom field in both ISO and DD/MM/YYYY', () => {
    const result = buildCardSearchStrings(buildCard({ values: [['date', '2026-09-10']] }));

    expect(result).toContain('2026-09-10');
    expect(result).toContain('10/09/2026');
  });

  test('includes a dropdown custom field by option name, not only its id', () => {
    const options = [
      { id: 'opt-1', name: 'Em andamento' },
      { id: 'opt-2', name: 'Concluído' },
    ];

    const result = buildCardSearchStrings(
      buildCard({ values: [['dropdown', 'opt-1', options]] }),
    );

    expect(result).toContain('em andamento');
    expect(result).toContain('concluído');
  });

  test('excludes checkbox values', () => {
    const result = buildCardSearchStrings(buildCard({ values: [['checkbox', 'true']] }));

    expect(result).not.toContain('true');
  });

  test('tolerates a card with no custom field values', () => {
    expect(() => buildCardSearchStrings({ name: 'a', description: null })).not.toThrow();
  });

  test('tolerates a malformed date value', () => {
    const result = buildCardSearchStrings(buildCard({ values: [['date', 'not-a-date']] }));

    expect(result).toContain('not-a-date');
  });
});
```

- [ ] **Step 2: Rodar o teste e verificar que falha**

Run: `cd client && npx jest src/utils/build-card-search-strings.test.js`
Expected: FAIL — `Cannot find module './build-card-search-strings'`.

- [ ] **Step 3: Criar o util do client**

Criar `client/src/utils/build-card-search-strings.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import CustomFieldTypes from '../constants/CustomFieldTypes';

const appendIfPresent = (result, value) => {
  if (value) {
    result.push(value.toLowerCase());
  }
};

const appendCustomFieldValue = (result, customFieldValueModel) => {
  const content = customFieldValueModel.content;
  const customField = customFieldValueModel.customField;

  if (!content || !customField) {
    return;
  }

  switch (customField.type) {
    case CustomFieldTypes.DATE: {
      appendIfPresent(result, content);

      // Stored as ISO, but users search for what they see: DD/MM/YYYY.
      const parts = content.split('-');
      if (parts.length === 3) {
        result.push(`${parts[2]}/${parts[1]}/${parts[0]}`);
      }

      break;
    }
    case CustomFieldTypes.DROPDOWN: {
      const options = (customField.config && customField.config.options) || [];
      const selectedOption = options.find((option) => option.id === content);

      if (selectedOption) {
        appendIfPresent(result, selectedOption.name);
      }

      break;
    }
    case CustomFieldTypes.CHECKBOX:
      break;
    default:
      appendIfPresent(result, content);
  }
};

// Returns every searchable string of a card, lowercased. Used by the board search
// so custom field values are matched alongside name and description.
export default (cardModel) => {
  const result = [];

  appendIfPresent(result, cardModel.name);
  appendIfPresent(result, cardModel.description);

  if (cardModel.customFieldValues) {
    cardModel.customFieldValues.toModelArray().forEach((customFieldValueModel) => {
      appendCustomFieldValue(result, customFieldValueModel);
    });
  }

  return result;
};
```

- [ ] **Step 4: Rodar o teste e verificar que passa**

Run: `cd client && npx jest src/utils/build-card-search-strings.test.js`
Expected: PASS — 8 testes.

- [ ] **Step 5: Aplicar o util na busca do client**

Em `client/src/models/Board.js`, adicionar o import junto aos demais:

```js
import buildCardSearchStrings from '../utils/build-card-search-strings';
```

Substituir o bloco `if (this.search) { ... }` (linhas 348-375) por:

```js
    if (this.search) {
      const searchStringsByCardId = cardModels.map((cardModel) => ({
        cardModel,
        searchStrings: buildCardSearchStrings(cardModel),
      }));

      if (this.search.startsWith('/')) {
        let searchRegex;
        try {
          searchRegex = new RegExp(this.search.substring(1), 'i');
        } catch {
          return [];
        }

        cardModels = searchStringsByCardId
          .filter(({ searchStrings }) => searchStrings.some((str) => searchRegex.test(str)))
          .map(({ cardModel }) => cardModel);
      } else {
        const searchParts = buildSearchParts(this.search);

        cardModels = searchStringsByCardId
          .filter(({ searchStrings }) =>
            searchParts.every((searchPart) =>
              searchStrings.some((str) => str.includes(searchPart)),
            ),
          )
          .map(({ cardModel }) => cardModel);
      }
    }
```

`buildSearchParts` continua sendo usado — mantenha o import existente.

- [ ] **Step 6: Rodar lint e suíte do client**

Run: `cd client && npm run lint`
Expected: exit 0.

Run: `cd client && npm test`
Expected: PASS.

- [ ] **Step 7: Escrever o teste do server que falha**

Criar `server/test/utils/custom-field-search.test.js`:

```js
const { expect } = require('chai');

const { buildCustomFieldSearchClause } = require('../../utils/custom-field-search');

describe('custom-field-search', () => {
  describe('#buildCustomFieldSearchClause', () => {
    it('starts the placeholders at the provided index', () => {
      const { sql } = buildCustomFieldSearchClause({ startIndex: 3 });

      expect(sql).to.include('$3');
      expect(sql).not.to.include('$2');
    });

    it('matches the card being filtered and excludes checkbox fields', () => {
      const { sql } = buildCustomFieldSearchClause({ startIndex: 1 });

      expect(sql).to.include('cfv.card_id = card.id');
      expect(sql).to.include("cf.type <> 'checkbox'");
    });

    it('guards the date cast with a format regex', () => {
      const { sql } = buildCustomFieldSearchClause({ startIndex: 1 });

      expect(sql).to.include("content ~ '^\\d{4}-\\d{2}-\\d{2}$'");
    });

    it('resolves dropdown option names through the custom field config', () => {
      const { sql } = buildCustomFieldSearchClause({ startIndex: 1 });

      expect(sql).to.include("jsonb_array_elements(cf.config -> 'options')");
      expect(sql).to.include("option ->> 'name' ILIKE");
    });

    it('uses exactly one placeholder, at the provided index', () => {
      const { sql } = buildCustomFieldSearchClause({ startIndex: 4 });

      const used = [...new Set((sql.match(/\$(\d+)/g) || []).map((p) => Number(p.slice(1))))];

      expect(used).to.deep.equal([4]);
    });
  });
});
```

> O contrato é: o fragmento usa **exatamente um** placeholder, `$startIndex`. Quem chama é
> responsável por empurrar o valor correspondente em `queryValues` — nada de array
> compartilhado.

- [ ] **Step 8: Rodar o teste e verificar que falha**

Run: `cd server && npx mocha test/utils/custom-field-search.test.js`
Expected: FAIL — `Cannot find module '../../utils/custom-field-search'`.

- [ ] **Step 9: Criar o util do server**

Criar `server/utils/custom-field-search.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

// Values stored in custom_field_value.content are free text, so the date cast has to
// be guarded by a format check — a bare `content::date` throws on any non-date row.
const DATE_FORMAT_REGEX = String.raw`^\d{4}-\d{2}-\d{2}$`;

// Matches cards that have a custom field value matching the search term. CHECKBOX is
// excluded: its content is only ever 'true' or 'false'. The fragment uses exactly one
// placeholder, $startIndex — the caller must push the term into queryValues.
const buildCustomFieldSearchClause = ({ startIndex }) => {
  const part = `$${startIndex}`;

  const sql = `EXISTS (
    SELECT 1
      FROM custom_field_value cfv
      JOIN custom_field cf ON cf.id = cfv.custom_field_id
     WHERE cfv.card_id = card.id
       AND cf.type <> 'checkbox'
       AND (
         cfv.content ILIKE '%' || ${part} || '%'
         OR (cf.type = 'date'
             AND cfv.content ~ '${DATE_FORMAT_REGEX}'
             AND to_char(cfv.content::date, 'DD/MM/YYYY') ILIKE '%' || ${part} || '%')
         OR (cf.type = 'dropdown'
             AND EXISTS (
               SELECT 1
                 FROM jsonb_array_elements(cf.config -> 'options') AS option
                WHERE option ->> 'id' = cfv.content
                  AND option ->> 'name' ILIKE '%' || ${part} || '%'
             ))
       )
  )`;

  return { sql };
};

module.exports = {
  buildCustomFieldSearchClause,
};
```

- [ ] **Step 10: Rodar o teste e verificar que passa**

Run: `cd server && npx mocha test/utils/custom-field-search.test.js`
Expected: PASS — 5 testes.

- [ ] **Step 11: Usar a cláusula na busca do server**

Em `server/api/hooks/query-methods/models/Card.js`, adicionar o import no topo (junto aos demais `require`):

```js
const { buildCustomFieldSearchClause } = require('../../../../utils/custom-field-search');
```

Substituir o bloco `if (search) { ... }` (linhas 75-91) por:

```js
    if (search) {
      if (search.startsWith('/')) {
        queryValues.push(search.substring(1));
        const termPlaceholder = `$${queryValues.length}`;

        // The regex branch searches the whole term; the custom field fragment
        // reuses the same placeholder so no extra value is needed.
        const { sql: customFieldSql } = buildCustomFieldSearchClause({
          startIndex: queryValues.length,
        });

        query += ` AND (card.name ~* ${termPlaceholder} OR card.description ~* ${termPlaceholder} OR ${customFieldSql})`;
      } else {
        const searchParts = buildSearchParts(search);

        if (searchParts.length > 0) {
          const ilikeValues = searchParts.map((searchPart) => {
            queryValues.push(searchPart);
            return `'%' || $${queryValues.length} || '%'`;
          });

          // Custom field values match ANY of the parts (a card would rarely carry
          // every part in a single field), unlike name/description which require all.
          const customFieldClauses = searchParts.map((searchPart) => {
            queryValues.push(searchPart);
            const { sql: customFieldSql } = buildCustomFieldSearchClause({
              startIndex: queryValues.length,
            });

            return customFieldSql;
          });

          query += ` AND ((card.name ILIKE ALL(ARRAY[${ilikeValues.join(', ')}])) OR (card.description ILIKE ALL(ARRAY[${ilikeValues.join(', ')}])) OR ${customFieldClauses.join(' OR ')})`;
        }
      }
    }
```

- [ ] **Step 12: Verificar lint e suíte do server**

Run: `cd server && npm run lint`
Expected: exit 0.

Run: `cd server && npm test`
Expected: PASS (inclui `test/utils/custom-field-search.test.js` e `test/utils/export-formatters.test.js`).

- [ ] **Step 13: Verificar manualmente a busca no board**

Com client e server rodando:

1. Criar um custom field de texto num card com o valor `HMMG-2026-00999`.
2. Abrir a busca do board e digitar `HMMG-2026-00999`.
3. Expected: o card aparece — antes não aparecia.
4. Criar um custom field de data com `10/09/2026` e buscar `10/09/2026`.
5. Expected: o card aparece.
6. Criar um dropdown com a opção `Em andamento`, selecioná-la, e buscar `andamento`.
7. Expected: o card aparece.
8. Buscar por um termo existente no `name` de outro card.
9. Expected: continua funcionando (nenhuma regressão).

- [ ] **Step 14: Commit**

```bash
git add client/src/utils/build-card-search-strings.js \
  client/src/utils/build-card-search-strings.test.js \
  client/src/models/Board.js \
  server/utils/custom-field-search.js \
  server/test/utils/custom-field-search.test.js \
  server/api/hooks/query-methods/models/Card.js
git commit -m "feat: busca do board passa a considerar custom fields"
```

---

## Task 4: Botão de copiar do custom field

**Files:**
- Create: `client/src/utils/copy-to-clipboard.js`
- Create: `client/src/utils/copy-to-clipboard.test.js`
- Modify: `client/src/components/custom-fields/CustomField/CustomField.jsx:78-89`

**Interfaces:**
- Consumes: nada
- Produces: `copyToClipboard(text: string) → Promise<boolean>` — resolve `true` só quando a cópia foi confirmada.

**Contexto:** `navigator.clipboard` só existe em contexto seguro (HTTPS ou `localhost`). Acesso por `http://<IP-da-LAN>:1337` deixa a API `undefined` e a chamada lança `TypeError` sem tratamento. Além disso o `setIsCopied(true)` atual roda de forma síncrona, antes da promise resolver — o ícone vira ✓ mesmo quando falhou.

- [ ] **Step 1: Escrever o teste que falha**

Criar `client/src/utils/copy-to-clipboard.test.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

import copyToClipboard from './copy-to-clipboard';

describe('copyToClipboard', () => {
  const originalClipboard = navigator.clipboard;
  const originalExecCommand = document.execCommand;

  afterEach(() => {
    Object.defineProperty(navigator, 'clipboard', {
      value: originalClipboard,
      configurable: true,
    });
    document.execCommand = originalExecCommand;
  });

  test('uses the async clipboard API when available', async () => {
    const writeText = jest.fn().mockResolvedValue(undefined);
    Object.defineProperty(navigator, 'clipboard', {
      value: { writeText },
      configurable: true,
    });

    await expect(copyToClipboard('hello')).resolves.toBe(true);
    expect(writeText).toHaveBeenCalledWith('hello');
  });

  test('falls back to execCommand when the clipboard API is missing', async () => {
    Object.defineProperty(navigator, 'clipboard', { value: undefined, configurable: true });
    document.execCommand = jest.fn().mockReturnValue(true);

    await expect(copyToClipboard('hello')).resolves.toBe(true);
    expect(document.execCommand).toHaveBeenCalledWith('copy');
  });

  test('resolves false when the clipboard API rejects', async () => {
    const writeText = jest.fn().mockRejectedValue(new Error('denied'));
    Object.defineProperty(navigator, 'clipboard', {
      value: { writeText },
      configurable: true,
    });

    await expect(copyToClipboard('hello')).resolves.toBe(false);
  });

  test('resolves false when execCommand reports failure', async () => {
    Object.defineProperty(navigator, 'clipboard', { value: undefined, configurable: true });
    document.execCommand = jest.fn().mockReturnValue(false);

    await expect(copyToClipboard('hello')).resolves.toBe(false);
  });

  test('removes the temporary textarea in every path', async () => {
    Object.defineProperty(navigator, 'clipboard', { value: undefined, configurable: true });
    document.execCommand = jest.fn().mockReturnValue(true);

    await copyToClipboard('hello');

    expect(document.querySelectorAll('textarea')).toHaveLength(0);
  });
});
```

- [ ] **Step 2: Rodar o teste e verificar que falha**

Run: `cd client && npx jest src/utils/copy-to-clipboard.test.js`
Expected: FAIL — `Cannot find module './copy-to-clipboard'`.

- [ ] **Step 3: Criar o util**

Criar `client/src/utils/copy-to-clipboard.js`:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

// navigator.clipboard only exists in a secure context, so instances served over
// plain HTTP on a LAN address need the legacy execCommand path.
const copyWithExecCommand = (text) => {
  const textarea = document.createElement('textarea');

  textarea.value = text;
  textarea.setAttribute('readonly', '');
  textarea.style.position = 'fixed';
  textarea.style.top = '-9999px';

  document.body.appendChild(textarea);

  let succeeded = false;
  try {
    textarea.select();
    succeeded = document.execCommand('copy');
  } catch {
    succeeded = false;
  } finally {
    document.body.removeChild(textarea);
  }

  return succeeded;
};

export default async (text) => {
  if (navigator.clipboard && navigator.clipboard.writeText) {
    try {
      await navigator.clipboard.writeText(text);
      return true;
    } catch {
      return false;
    }
  }

  return copyWithExecCommand(text);
};
```

- [ ] **Step 4: Rodar o teste e verificar que passa**

Run: `cd client && npx jest src/utils/copy-to-clipboard.test.js`
Expected: PASS — 5 testes.

- [ ] **Step 5: Usar o util no componente e só marcar sucesso de verdade**

Em `client/src/components/custom-fields/CustomField/CustomField.jsx`, adicionar o import:

```js
import copyToClipboard from '../../../utils/copy-to-clipboard';
```

Substituir o `handleCopyClick` (linhas 78-89) por:

```js
  const handleCopyClick = useCallback(async () => {
    if (isCopied) {
      return;
    }

    const succeeded = await copyToClipboard(customFieldValue.content);

    if (!succeeded) {
      return;
    }

    setIsCopied(true);
    setTimeout(() => {
      setIsCopied(false);
    }, 1000);
  }, [customFieldValue, isCopied]);
```

- [ ] **Step 6: Verificar lint e suíte do client**

Run: `cd client && npm run lint`
Expected: exit 0.

Run: `cd client && npm test`
Expected: PASS.

- [ ] **Step 7: Verificar manualmente pelo endereço de rede**

1. Subir o client em `http://localhost:1337` → clicar em copiar num custom field de texto → Expected: ícone vira ✓ e o valor cola corretamente.
2. Subir o client e acessar por `http://<IP-da-LAN>:1337` (contexto **não** seguro) → clicar em copiar → Expected: ícone vira ✓ e o valor cola corretamente. Antes desta correção, nada era copiado.

- [ ] **Step 8: Commit**

```bash
git add client/src/utils/copy-to-clipboard.js \
  client/src/utils/copy-to-clipboard.test.js \
  client/src/components/custom-fields/CustomField/CustomField.jsx
git commit -m "fix: copiar valor de custom field funciona fora de contexto seguro"
```

---

## Task 5: Mockup do layout do PDF (gate)

**Files:**
- Create: `docs/mockup-pdf-export.html`

**Interfaces:**
- Consumes: nada
- Produces: nada executável. É um artefato de validação — as decisões visuais aprovadas aqui viram os valores das Tasks 6.

**Contexto:** A investigação do PDF de referência (`docs/board-export-2026-09-10.pdf`, lendo os streams de conteúdo) encontrou 4 defeitos. Este mockup existe para validar o layout corrigido antes de mexer no gerador.

**GATE: nenhum arquivo de `server/utils/export-formatters.js` é alterado antes do usuário aprovar este mockup.**

- [ ] **Step 1: Criar o mockup**

Criar `docs/mockup-pdf-export.html`. É um HTML autocontido (sem CDN, sem fetch): todo CSS inline, sem JavaScript externo. Deve seguir a estrutura abaixo.

**Seção 1 — Enunciado dos defeitos.** Uma tabela curta com os 4 problemas e onde ocorrem no PDF de referência.

**Seção 2 — Comparação antes/depois.** Lado a lado, o mesmo card renderizado:
- *Antes:* pill do label `INEX` com o texto quebrando em duas linhas e a segunda fora do fundo; descrição com `**negrito**` e `[texto](url)` literais; URL do SEI ocupando 4 linhas e invadindo a seção seguinte.
- *Depois:* pill com largura calculada com folga, truncado com ellipsis quando exceder a largura do card; descrição em texto plano com no máximo 4 linhas e ellipsis; link reduzido a `texto (url-curta)`.

**Seção 3 — Labels com a cor real.** Uma grade com **todos os 42 labels** de `client/src/constants/LabelColors.js`, cada um renderizado como pill com:
- o **texto do nome dentro**, sobre a cor de fundo real daquele label (não um bloco de cor sem texto);
- a cor de texto (claro/escuro) escolhida por luminância do fundo;
- o nome do label abaixo do pill, em texto pequeno.

Use o mapa abaixo (extraído de `client/src/styles.module.scss`). As duas últimas entradas são gradientes no CSS — o mockup propõe um hex sólido equivalente, que é o que `pdfkit` consegue desenhar:

```js
const LABEL_COLORS = {
  'muddy-grey': '#69655a', 'autumn-leafs': '#c9b037', 'morning-sky': '#52b9d5',
  'antique-blue': '#6c99bb', 'egg-yellow': '#f9c423', 'desert-sand': '#fad371',
  'dark-granite': '#8b8680', 'fresh-salad': '#ced85e', 'lagoon-blue': '#109dc0',
  'midnight-blue': '#0a63a0', 'light-orange': '#fdae5f', 'pumpkin-orange': '#ed9223',
  'light-concrete': '#afb0a4', 'sunny-grass': '#beca02', 'navy-blue': '#1d7299',
  'lilac-eyes': '#406cbd', 'apricot-red': '#fc736c', 'orange-peel': '#de692f',
  'silver-glint': '#adadad',        // gradiente no SCSS → 1ª parada
  'bright-moss': '#96b352', 'deep-ocean': '#004c70', 'summer-sky': '#5d9cec',
  'berry-red': '#e83855', 'light-cocoa': '#a85540', 'grey-stone': '#aab2bd',
  'tank-green': '#8aa177', 'coral-green': '#2b6a6c', 'sugar-plum': '#7e86c7',
  'pink-tulip': '#e34f7c', 'shady-rust': '#87564a', 'wet-rock': '#83949b',
  'wet-moss': '#4a8753', 'turquoise-sea': '#00858a', 'lavender-fields': '#b287bd',
  'piggy-red': '#f97394', 'light-mud': '#c7a57a', 'gun-metal': '#4f6573',
  'modern-green': '#77ce87', 'french-coast': '#00b4b1', 'sweet-lilac': '#975298',
  'red-burgundy': '#ad5f7d',
  'pirate-gold': '#b47e11',         // gradiente no SCSS → 2ª parada
};
```

Cor de texto por luminância relativa (WCAG), limiar `0.45`:

```js
const luminance = (hex) => {
  const c = hex.replace('#', '');
  const channel = (v) => {
    const s = parseInt(v, 16) / 255;
    return s <= 0.03928 ? s / 12.92 : ((s + 0.055) / 1.055) ** 2.4;
  };
  return (
    0.2126 * channel(c.slice(0, 2)) +
    0.7152 * channel(c.slice(2, 4)) +
    0.0722 * channel(c.slice(4, 6))
  );
};

const textColorFor = (hex) => (luminance(hex) > 0.45 ? '#1A1A18' : '#FFFFFF');
```

**Seção 4 — Header e resumo.** O cabeçalho do PDF (título, nota de exclusão, "Gerado em") e a grade de resumo por lista, com **múltiplas linhas de label** mostradas para provar que o cálculo de altura acompanha a quebra.

- [ ] **Step 2: Abrir o mockup e conferir os 42 labels**

Abrir `docs/mockup-pdf-export.html` no navegador.

Expected:
- 42 pills, cada um com o **nome legível dentro** do pill;
- nenhum texto saindo da caixa do pill;
- pills de fundo claro com texto escuro e vice-versa;
- a comparação antes/depois mostra o `INEX` corrigido.

- [ ] **Step 3: Commit do mockup**

```bash
git add docs/mockup-pdf-export.html
git commit -m "docs: mockup do layout corrigido do export PDF"
```

- [ ] **Step 4: PAUSA — aprovação do usuário**

Apresentar o mockup ao usuário e **parar**. Não iniciar a Task 6 até haver aprovação explícita.

Perguntas a levar: (a) os dois hexes escolhidos para os gradientes (`silver-glint` → `#adadad`, `pirate-gold` → `#b47e11`) estão bons? (b) o limite de 4 linhas para a descrição é adequado? (c) o formato `texto (url-curta)` para links está bom?

---

## Task 6: Corrigir o gerador de PDF

**Files:**
- Create: `server/utils/label-colors.js`
- Modify: `server/utils/export-formatters.js`
- Modify: `server/test/utils/export-formatters.test.js`

**Interfaces:**
- Consumes: mockup aprovado na Task 5.
- Produces:
  - `SERVER_LABEL_COLORS: Record<string, string>` — nome da cor → hex sólido
  - `textColorFor(hex: string) → '#1A1A18' | '#FFFFFF'`
  - `toPlainText(markdown: string) → string` — markdown → texto plano
  - `truncate(value, maxLength)` — já existe, mantida

**Contexto:** Quatro defeitos no `drawDetailCard` de `server/utils/export-formatters.js`:
- (a) o pill mede `widthOfString(name) + 14` e renderiza com `width: pillW - 14` — largura exata, sem folga de padding real, o que faz o `pdfkit` quebrar a linha para fora do fundo (evidência: `INEX` renderizado como `INE` / `X`);
- (b) tokens longos sem espaço (URLs do SEI com ~400 caracteres) quebram o layout;
- (c) markdown é escrito cru (`**negrito**`, `[texto](url)`);
- (d) `label.color` é ignorado — todos os pills saem verdes (`COLORS.labelBg`).

- [ ] **Step 1: Escrever os testes que falham**

Adicionar ao final de `server/test/utils/export-formatters.test.js` (não remover os testes existentes):

```js
const { toPlainText } = require('../../utils/export-formatters');
const { SERVER_LABEL_COLORS, textColorFor } = require('../../utils/label-colors');

describe('label-colors', () => {
  it('maps the label color name to a solid hex', () => {
    expect(SERVER_LABEL_COLORS['berry-red']).to.equal('#e83855');
  });

  it('maps the gradient colors to a solid fallback', () => {
    expect(SERVER_LABEL_COLORS['pirate-gold']).to.equal('#b47e11');
    expect(SERVER_LABEL_COLORS['silver-glint']).to.equal('#adadad');
  });

  it('picks a dark text color on a light background', () => {
    expect(textColorFor('#f9c423')).to.equal('#1A1A18');
  });

  it('picks a light text color on a dark background', () => {
    expect(textColorFor('#004c70')).to.equal('#FFFFFF');
  });
});

describe('#toPlainText', () => {
  it('strips bold and italic markers', () => {
    expect(toPlainText('**Processo anterior:** nada')).to.equal('Processo anterior: nada');
    expect(toPlainText('*itálico* aqui')).to.equal('itálico aqui');
  });

  it('leaves underscores inside identifiers alone', () => {
    expect(toPlainText('HMMG_2026_001')).to.equal('HMMG_2026_001');
  });

  it('turns a markdown link into text plus its url', () => {
    expect(toPlainText('[Protocolo](https://exemplo.test/a)')).to.equal(
      'Protocolo (https://exemplo.test/a)',
    );
  });

  it('leaves plain text untouched', () => {
    expect(toPlainText('HMMG.2026.00001721-57')).to.equal('HMMG.2026.00001721-57');
  });

  it('tolerates an empty value', () => {
    expect(toPlainText('')).to.equal('');
    expect(toPlainText(null)).to.equal('');
  });
});
```

E no `describe('#toPDF')` existente, adicionar:

```js
  it('does not throw on a description with a very long unbroken token', async () => {
    const longUrl = `https://sei.exemplo.test/controlador.php?${'a'.repeat(400)}`;
    const pdf = await toPDF({
      ...mockData,
      details: [
        {
          listName: 'Backlog',
          card: { ...mockData.details[0].card, description: `Veja ${longUrl}` },
        },
      ],
    });

    expect(pdf.slice(0, 4).toString()).to.equal('%PDF');
  });

  it('does not throw on a label name longer than the card width', async () => {
    const pdf = await toPDF({
      ...mockData,
      details: [
        {
          listName: 'Backlog',
          card: {
            ...mockData.details[0].card,
            labels: [{ id: 'l1', name: 'L'.repeat(200), color: 'berry-red' }],
          },
        },
      ],
    });

    expect(pdf.slice(0, 4).toString()).to.equal('%PDF');
  });
```

- [ ] **Step 2: Rodar os testes e verificar que falham**

Run: `cd server && npx mocha test/utils/export-formatters.test.js`
Expected: FAIL — `Cannot find module '../../utils/label-colors'` e `toPlainText is not a function`.

- [ ] **Step 3: Criar o mapa de cores dos labels**

Criar `server/utils/label-colors.js` com o mapa completo das 42 cores (os mesmos valores do mockup aprovado na Task 5) e as duas funções:

```js
/*!
 * Copyright (c) 2024 PLANKA Software GmbH
 * Licensed under the Fair Use License: https://github.com/plankanban/planka/blob/master/LICENSE.md
 */

// Mirrors client/src/constants/LabelColors.js. Values come from the .background*
// rules in client/src/styles.module.scss; `silver-glint` and `pirate-gold` are
// gradients there and are flattened to a representative solid color so pdfkit,
// which has no gradient fill, can draw them.
const SERVER_LABEL_COLORS = {
  'muddy-grey': '#69655a',
  'autumn-leafs': '#c9b037',
  'morning-sky': '#52b9d5',
  'antique-blue': '#6c99bb',
  'egg-yellow': '#f9c423',
  'desert-sand': '#fad371',
  'dark-granite': '#8b8680',
  'fresh-salad': '#ced85e',
  'lagoon-blue': '#109dc0',
  'midnight-blue': '#0a63a0',
  'light-orange': '#fdae5f',
  'pumpkin-orange': '#ed9223',
  'light-concrete': '#afb0a4',
  'sunny-grass': '#beca02',
  'navy-blue': '#1d7299',
  'lilac-eyes': '#406cbd',
  'apricot-red': '#fc736c',
  'orange-peel': '#de692f',
  'silver-glint': '#adadad',
  'bright-moss': '#96b352',
  'deep-ocean': '#004c70',
  'summer-sky': '#5d9cec',
  'berry-red': '#e83855',
  'light-cocoa': '#a85540',
  'grey-stone': '#aab2bd',
  'tank-green': '#8aa177',
  'coral-green': '#2b6a6c',
  'sugar-plum': '#7e86c7',
  'pink-tulip': '#e34f7c',
  'shady-rust': '#87564a',
  'wet-rock': '#83949b',
  'wet-moss': '#4a8753',
  'turquoise-sea': '#00858a',
  'lavender-fields': '#b287bd',
  'piggy-red': '#f97394',
  'light-mud': '#c7a57a',
  'gun-metal': '#4f6573',
  'modern-green': '#77ce87',
  'french-coast': '#00b4b1',
  'sweet-lilac': '#975298',
  'red-burgundy': '#ad5f7d',
  'pirate-gold': '#b47e11',
};

const FALLBACK_LABEL_COLOR = '#aab2bd';
const DARK_TEXT = '#1A1A18';
const LIGHT_TEXT = '#FFFFFF';
const LUMINANCE_THRESHOLD = 0.45;

const luminance = (hex) => {
  const c = hex.replace('#', '');
  const channel = (value) => {
    const srgb = parseInt(value, 16) / 255;
    return srgb <= 0.03928 ? srgb / 12.92 : ((srgb + 0.055) / 1.055) ** 2.4;
  };

  return (
    0.2126 * channel(c.slice(0, 2)) +
    0.7152 * channel(c.slice(2, 4)) +
    0.0722 * channel(c.slice(4, 6))
  );
};

const getLabelColor = (colorName) => SERVER_LABEL_COLORS[colorName] || FALLBACK_LABEL_COLOR;

const textColorFor = (hex) => (luminance(hex) > LUMINANCE_THRESHOLD ? DARK_TEXT : LIGHT_TEXT);

module.exports = {
  SERVER_LABEL_COLORS,
  getLabelColor,
  textColorFor,
};
```

- [ ] **Step 4: Adicionar `toPlainText` e as correções ao `export-formatters.js`**

Em `server/utils/export-formatters.js`:

Adicionar os requires:

```js
const { getLabelColor, textColorFor } = require('./label-colors');
```

Adicionar `toPlainText` logo após `truncate`:

```js
const MARKDOWN_LINK_REGEX = /\[([^\]]+)\]\(([^)]+)\)/g;
// Deliberately excludes single-underscore emphasis: `_` shows up inside real
// identifiers (HMMG_2026_001) and stripping it would corrupt the text.
const BOLD_ITALIC_REGEX = /(\*\*|__)(.*?)\1|\*([^*]+)\*/g;

// The PDF draws plain text, so markdown syntax has to go. Links keep their target
// so the reader can still reach the URL.
const toPlainText = (value) => {
  if (!value) {
    return '';
  }

  return String(value)
    .replace(MARKDOWN_LINK_REGEX, '$1 ($2)')
    .replace(BOLD_ITALIC_REGEX, (match, boldMarker, boldText, italicText) => {
      if (boldText !== undefined && boldText !== null) {
        return boldText;
      }

      return italicText !== undefined && italicText !== null ? italicText : match;
    })
    .trim();
};

// pdfkit wraps on spaces only, so a long unbroken token (typically a URL) would
// otherwise run past the card. Insert break opportunities every N characters.
const breakLongTokens = (value, maxTokenLength = 40) => {
  if (!value) {
    return '';
  }

  return String(value)
    .split(/(\s+)/)
    .map((token) => {
      if (token.length <= maxTokenLength || /^\s+$/.test(token)) {
        return token;
      }

      const chunks = [];
      for (let i = 0; i < token.length; i += maxTokenLength) {
        chunks.push(token.slice(i, i + maxTokenLength));
      }

      return chunks.join(' ');
    })
    .join('');
};
```

Substituir a medição de `pillWidths` e as duas renderizações de pill dentro de `drawDetailCard`, e trocar a medição/renderização da descrição:

```js
      doc.font('Helvetica').fontSize(9);
      const plainDescription = breakLongTokens(toPlainText(card.description));

      const descText = plainDescription || 'Sem descrição';
      let descH = doc.heightOfString(descText, { width: innerW, lineGap: 2 });

      // Cap the description at 4 lines so one pathological card cannot push the rest
      // of the page. The ellipsis is applied at draw time via the height option.
      const maxDescH = doc.heightOfString('X\nX\nX\nX', { width: innerW, lineGap: 2 });
      const isDescriptionTruncated = descH > maxDescH;
      if (isDescriptionTruncated) {
        descH = maxDescH;
      }
```

E o bloco de desenho da descrição passa a ser:

```js
      if (card.description) {
        doc.fillColor(COLORS.ink);
        doc.text(descText, innerX, y + 4, {
          width: innerW,
          lineGap: 2,
          height: descH,
          ellipsis: isDescriptionTruncated,
        });
      } else {
        doc.font('Helvetica-Oblique').fillColor(COLORS.muted);
        doc.text('Sem descrição', innerX, y + 4, { width: innerW });
      }
```

Para os pills de label, substituir a medição por uma versão com padding real e truncamento:

```js
      doc.font('Helvetica').fontSize(8);
      const PILL_PAD = 8;
      const MAX_PILL_W = innerW;

      const labelTexts = card.labels.map((label) =>
        truncate(label.name, Math.max(4, Math.floor((MAX_PILL_W - PILL_PAD * 2) / 4.6))),
      );

      const pillWidths = labelTexts.map((text) => doc.widthOfString(text) + PILL_PAD * 2);
```

E o desenho dos pills passa a usar `labelTexts`, a cor real e a cor de texto calculada:

```js
      if (card.labels.length > 0) {
        let pillX = innerX;
        let pillRowY = y;

        card.labels.forEach((label, index) => {
          const pillW = Math.min(pillWidths[index], MAX_PILL_W);
          const background = getLabelColor(label.color);
          const foreground = textColorFor(background);

          if (pillX + pillW > innerX + innerW) {
            pillX = innerX;
            pillRowY += 18;
          }

          doc.roundedRect(pillX, pillRowY, pillW, 14, 7).fill(background);
          doc.font('Helvetica').fontSize(8).fillColor(foreground);
          doc.text(labelTexts[index], pillX + PILL_PAD, pillRowY + 3, {
            width: pillW - PILL_PAD * 2,
            align: 'center',
          });
          pillX += pillW + 4;
        });

        y += labelsH;
      } else {
```

E o cálculo de `labelsH` acima passa a usar as mesmas larguras:

```js
      let labelRows = 1;
      let used = 0;
      pillWidths.forEach((pillW) => {
        const width = Math.min(pillW, MAX_PILL_W);

        if (used + width > innerW) {
          labelRows += 1;
          used = width + 4;
        } else {
          used += width + 4;
        }
      });
```

Por fim, exportar `toPlainText` no `module.exports`:

```js
module.exports = {
  formatDateForReport,
  toCSV,
  toPDF,
  toPlainText,
};
```

- [ ] **Step 5: Rodar os testes e verificar que passam**

Run: `cd server && npx mocha test/utils/export-formatters.test.js`
Expected: PASS — todos os testes antigos continuam passando e os novos também.

- [ ] **Step 6: Verificar lint do server**

Run: `cd server && npm run lint`
Expected: exit 0.

- [ ] **Step 7: Gerar um PDF real e comparar com o de referência**

1. Exportar em PDF o mesmo board que gerou `docs/board-export-2026-09-10.pdf`.
2. Abrir o PDF novo.
3. Expected:
   - cada pill de label mostra o nome legível **sobre a cor real do label**;
   - o label `INEX` aparece inteiro, sem quebrar para fora do fundo;
   - a descrição com a URL do SEI fica em no máximo 4 linhas, terminando em `…`, sem invadir a seção de labels;
   - nenhum `**` ou `[...](...)` aparece como texto literal.

- [ ] **Step 8: Commit**

```bash
git add server/utils/label-colors.js server/utils/export-formatters.js \
  server/test/utils/export-formatters.test.js
git commit -m "fix: export PDF usa cor real das labels, trunca e trata markdown"
```

---

## Verificação final

- [ ] `cd client && npm test` → PASS
- [ ] `cd client && npm run lint` → exit 0
- [ ] `cd server && npm test` → PASS
- [ ] `cd server && npm run lint` → exit 0
- [ ] Nenhuma data em formato americano visível: conferir due date do card, chip de vencimento, activity log, comentários e custom field de data
- [ ] Busca do board acha por custom field de texto, número, data e dropdown
- [ ] Botão de copiar funciona em `localhost` **e** pelo IP da LAN
- [ ] PDF exportado sem cortes, com labels coloridas e nome legível
- [ ] `git log --oneline master..fixes` mostra 6 commits, um por task

## Pendências conhecidas (fora de escopo)

- O título longo do header do PDF ainda pode colidir com a nota "Excluídos cards fechados..." à direita, que é desenhada na mesma linha do título. Não faz parte dos 4 defeitos investigados.
- A busca em custom fields não é indexada; em boards muito grandes o `EXISTS` pode ficar lento. Validar com um board real antes de considerar otimização.
